#include <stdio.h>
#include <stdlib.h>

extern char **environ;

int main(int argc, char const *argv[])
{
    int i;
    char *str = malloc(sizeof(10));
    static long s;
    getchar();
    exit(EXIT_SUCCESS);
}
